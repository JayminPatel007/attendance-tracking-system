/**
 * Analytics domain layer: Re-engagement Candidate calculator (ADR-0010),
 * read-model projection contracts, dashboard metric definitions. Most likely
 * future extraction candidate per ADR-0008/0015 — projections must be built
 * from domain events, never ad-hoc joins on transactional tables.
 */
package org.sabha.analytics.domain;
