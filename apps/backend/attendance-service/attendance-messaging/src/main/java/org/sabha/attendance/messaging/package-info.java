/**
 * Attendance context — Messaging adapter (Clean Architecture interface-adapters ring).
 *
 * <p>Currently empty. Scaffolded per ADR-0019's "all five modules per context
 * even when empty" policy so the first slice needing async messaging doesn't
 * have to restructure the build.</p>
 */
package org.sabha.attendance.messaging;
