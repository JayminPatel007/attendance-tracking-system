/**
 * Identity application layer: use-case interactors (Login, ChangePassword,
 * AssignRole, RevokeRole) that orchestrate the domain via inbound and outbound
 * ports. Framework-free — concrete adapters live in identity-infrastructure.
 */
package org.sabha.identity.application;
