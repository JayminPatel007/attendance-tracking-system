/**
 * Interface-adapters ring (Clean Architecture) for the Identity context:
 * messaging adapters. Empty by design — scaffolded per ADR-0019's "all 5
 * modules per context even when empty" policy.
 */
package org.sabha.identity.messaging;
