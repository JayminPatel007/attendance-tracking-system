/**
 * Sabha domain layer: Sabha + Occurrence aggregates, Roster, Occurrence state
 * machine (ADR-0001), schedule shape rules for weekly vs monthly-ad-hoc
 * Sabhas (ADR-0012). Pure domain — no Spring, no JPA.
 */
package org.sabha.sabha.domain;
