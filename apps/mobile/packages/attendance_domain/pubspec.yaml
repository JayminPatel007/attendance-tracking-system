name: attendance_domain
description: Mobile mirror of the backend attendance bounded context. Pure Dart.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  shared_kernel:
    path: ../shared_kernel

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
