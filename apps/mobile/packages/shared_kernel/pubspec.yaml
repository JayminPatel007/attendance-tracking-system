name: shared_kernel
description: Cross-context value objects and types. Pure Dart — no Flutter dependency.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
