name: sabha_mobile_workspace
description: Melos workspace root for the Sabha Attendance mobile app + packages.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  melos: ^6.0.0
