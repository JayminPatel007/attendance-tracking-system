/**
 * attendance-domain — frontend mirror of the backend attendance bounded context.
 * Types for AttendanceMarking, MarkingType. (Web does not capture markings —
 * per ADR-0003 — but reads them for analytics.)
 *
 * Placeholder for Slice 1.
 */
export const __ATTENDANCE_DOMAIN__ = 'attendance-domain';
