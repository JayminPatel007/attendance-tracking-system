/**
 * identity-domain — frontend mirror of the backend identity bounded context.
 * Types and interfaces (User, Role, Session) used by identity feature libraries.
 *
 * Placeholder for Slice 1.
 */
export const __IDENTITY_DOMAIN__ = 'identity-domain';
